package cn.xupt.item.pojo;

import cn.xupt.pojo.TbItem;

public class Item extends TbItem {

	public String[] getImages(){
		String image2 = this.getImages();
		if(image2 != null && "".equals(image2)){
			String[] strings = image2.split(",");
			return strings;
		}
	}
}
